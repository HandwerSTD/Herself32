#include <windows.h>

int WINAPI WinMain(
	HINSTANCE hInst,
	HINSTANCE hPrevInst,
	LPTSTR lpCmdLine,
	int nCmdShow) {
	MessageBox(nullptr, TEXT("Herself32 %%%"), TEXT("%%%"), MB_OK);
	return 0;
}